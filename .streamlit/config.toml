[browser]
gatherUsageStats = false

[server]
# Cap uploads (MB) for audio/images in this app; raise if you need longer recordings.
maxUploadSize = 50

[theme]
base = "light"
primaryColor = "#2563EB"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F8FAFC"
textColor = "#0F172A"
font = "sans serif"

[client]
# Logo lives in secrets under [branding] logo_path / logo_url — see .streamlit/secrets.toml.example
